version: '3.1'

services:

  mongo:
    image: mongo
    container_name: mongoDb
    restart: always
    environment:
      MONGO_INITDB_ROOT_USERNAME: bd2
      MONGO_INITDB_ROOT_PASSWORD: bd2
    ports:
      - 27017:27017
